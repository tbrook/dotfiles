
# 
source_f() {
    if [ -f "$1" ]; then
	. "$@"
    else
	echo "'$1' は FILE ではありません" 1>&2
    fi
}


# 
: "${DOTPATH:=${HOME}/.dotfiles}"
export DOTPATH


# 
if [[ ! -d "${SOFTWARE_PATH:=${HOME}/software-space}" ]]; then
    SOFTWARE_PATH="${HOME}"
fi

export SOFTWARE_PATH



# Local variables:
# mode: shell-script
# End:
